services:
  - type: web
    name: career-role-predictor
    env: python
    region: oregon  # or any available Render region
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
